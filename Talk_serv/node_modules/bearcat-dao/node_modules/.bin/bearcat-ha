#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../bearcat-ha/bin/bearcat-ha" "$@"
  ret=$?
else 
  node  "$basedir/../bearcat-ha/bin/bearcat-ha" "$@"
  ret=$?
fi
exit $ret
